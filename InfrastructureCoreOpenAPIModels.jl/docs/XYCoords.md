# XYCoords

A single point, given as its `x` and `y` coordinates.

## Properties
Name | Type | Description | Notes
------------ | ------------- | ------------- | -------------
**`x`** | **`Float64`** | Units: the wrapped function's input unit. | [required]
**`y`** | **`Float64`** | Units: the wrapped function's output unit. | [required]
