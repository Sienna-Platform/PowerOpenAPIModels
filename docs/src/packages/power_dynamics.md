# PowerDynamicsOpenAPIModels

The dynamics domain: machine, exciter, governor, and inverter-control models. A dynamic model
attaches to a static injector through that injector's `dynamic_injector` id.

```@autodocs
Modules = [PowerDynamicsOpenAPIModels]
```
