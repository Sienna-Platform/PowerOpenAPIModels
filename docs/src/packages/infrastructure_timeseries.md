# InfrastructureTimeSeriesOpenAPIModels

The six canonical time series association types and the `TimeSeriesAssociation` wrapper that
selects among them on the `time_series_type` discriminator. An association is metadata: it
names where the values live, and never carries the array itself.

```@autodocs
Modules = [InfrastructureTimeSeriesOpenAPIModels]
```
