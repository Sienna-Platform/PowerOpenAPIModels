# PowerCoreOpenAPIModels

The power-system types shared across operations, investments, and dynamics: topology, the
cost and value curve family, and the power enums. A type lives here rather than in a domain
package when more than one domain references it.

```@autodocs
Modules = [PowerCoreOpenAPIModels]
```
