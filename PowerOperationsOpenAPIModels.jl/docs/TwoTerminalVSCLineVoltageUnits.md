# TwoTerminalVSCLineVoltageUnits

Unit basis for the DC bus voltage limits (voltage_limits_from/to only). Independent of setpoint_voltage_units, which covers dc_setpoint_from/to and ac_setpoint_from/to.

## Allowed values
- `NATURAL_UNITS`
- `COMPONENT_BASE`
